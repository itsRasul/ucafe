import { MigrationInterface,QueryRunner } from "typeorm";
export class CreateNotificationOutbox1787774400000 implements MigrationInterface { name="CreateNotificationOutbox1787774400000"; async up(q:QueryRunner){await q.query(`CREATE TYPE notification_delivery_status AS ENUM ('PENDING','PROCESSING','SENT','FAILED')`);await q.query(`CREATE TABLE notification_deliveries(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),coffee_shop_id uuid NOT NULL REFERENCES coffee_shops(id) ON DELETE CASCADE,reservation_id uuid NOT NULL REFERENCES reservations(id) ON DELETE CASCADE,type varchar(50) NOT NULL,recipient_ciphertext text NOT NULL,payload jsonb NOT NULL,status notification_delivery_status NOT NULL DEFAULT 'PENDING',attempts smallint NOT NULL DEFAULT 0,next_attempt_at timestamptz NOT NULL DEFAULT now(),provider_message_id varchar(100),last_error_code varchar(50),sent_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),CONSTRAINT uq_notification_delivery UNIQUE(reservation_id,type),CONSTRAINT ck_notification_attempts CHECK(attempts BETWEEN 0 AND 3))`);await q.query(`CREATE INDEX idx_notification_dispatch ON notification_deliveries(status,next_attempt_at) WHERE status='PENDING'`);} async down(q:QueryRunner){await q.query(`DROP TABLE notification_deliveries`);await q.query(`DROP TYPE notification_delivery_status`);} }
