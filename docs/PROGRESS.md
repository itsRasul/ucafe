# Project continuation checkpoint

Last updated: 2026-09-25

## Discounts & Promotions Phase 2

Added optional weekly windows to the existing Promotion eligibility path, used by public menu pricing, cart/checkout pricing, and coupon validation. Windows support canonical weekdays, multiple and overlapping ranges, overnight periods, and explicit all-day windows in the tenant's café timezone. Existing UTC campaign dates, Phase 0/1 promotions without schedules, minimums, usage limits, redemption transactions, conflict resolution, and order snapshots remain in the existing engine. Owners configure schedules in the current Promotions form; no activation job or shared resolved-price cache was added.

Migration `1790431200000-AddPromotionScheduleWindows` adds tenant-scoped child rows and is applied to the development database. Verification: workspace typecheck and build passed; full API suite passed (116 passed, 13 database-only skips) with promotion PostgreSQL integration enabled; focused schedule/pricing checks passed (8/8), and the final promotion PostgreSQL integration passed (2/2), including public menu pricing, coupon minimum composition, schedule expiry, and immutable order snapshots. Migration show reports the new migration executed; API readiness and `/admin/promotions` returned 200. The browser reached the owner OTP login screen, so authenticated admin interaction/visual checks were unavailable. There is no lint command. `docs/PROJECT_SPEC.md` and `docs/PLAN.md` were absent; current PRD, Business Rules, Architecture, Decisions, Development, Testing, Ordering, Database, and Discounts docs were used.

## Discounts & Promotions Phase 1

Extended the Phase 0 promotion pricing path with entire-order targets, coupon activation, post-item-discount minimums, percentage caps, tenant-scoped code normalization, authenticated coupon quotes, transactional offline-order redemptions, and under-review cancellation release. Migration `1790427600000-AddPromotionCoupons` adds coupon/redemption records and immutable order-stage snapshots. The owner Promotions screen now configures order targets and coupon limits; checkout applies/removes one code and displays item/order discount amounts. Customer and owner order details show the snapshotted breakdown. Subscription Zarinpal billing is separate; customer order payment remains offline only.

Baseline before edits: 111 passed, 13 skipped; workspace typecheck and production build passed. The new migration applied to development PostgreSQL and `migration:show` confirms it. Final full API suite: 111 passed, 15 skipped (the two new database cases run separately); focused PostgreSQL integration: 2/2 passed, covering capped/item/automatic/targeted rewards, minimums, dates, activation, tenant scope, quote/order totals, idempotency, release, and final-use concurrency. Workspace typecheck and production build passed; the live restarted API returned the new quote breakdown and 401 for unauthenticated coupon quotes. The public example tenant's menu/cart were inspected in-browser, but owner and authenticated checkout visuals were unavailable without a session. There is no lint command.

## Analytics Phase 5: order and channel analytics

Implemented `GET /tenant/analytics/orders` and the **سفارش‌ها و کانال‌ها** tab. The report compares created orders, terminal outcomes, completion/cancellation rates, canceled order value, average items and size buckets, status trends, pickup/courier performance, and source performance. Migration `1790331236284` adds nullable typed source tracking for public-client checkout; existing rows remain `UNKNOWN`.

Completion/cancellation rates use terminal outcomes only (`DELIVERED + CANCELED`); active statuses do not count as failures. The order source intentionally identifies the public-client flow only; it cannot distinguish website vs QR, admin-created orders do not exist, and no visits, structured reasons, rejected state, or transition history are stored. See [ANALYTICS.md](ANALYTICS.md) for formulas, query strategy, unknown-source handling, and deferred work.

Verification: workspace typecheck and production build passed. The focused Analytics and order/inventory PostgreSQL integrations passed (16/16); the serial full API suite passed (113 passed, 5 skipped), with the Analytics PostgreSQL cases run separately against the development database. The dynamic plan-editor database fixture was skipped because local plan settings are customized; Analytics feature resolver and service-denial tests passed. Migration `1790331236284` is applied and `migration:show` confirms all migrations. API route returned expected unauthenticated 401; `/admin/analytics` returned 200. Authenticated visual/mobile interaction review remains unavailable without a tenant-admin session.

## Analytics Phase 4: customer analytics

Implemented `/tenant/analytics/customers` with cafe-scoped identity, first-purchase new/returning classification, period comparisons and trends, reorder interval, lifetime repeat distribution, coverage, top-10 revenue/order rankings, and a Persian RTL customer analytics tab. Migration `1787883600000` adds the tenant/status/client/time index.

Verification: workspace typecheck and production build passed; full API suite passed (105 passed, 11 database-only cases skipped); Analytics and subscription-feature integration checks passed 19/19 on a fresh database migrated through all 41 migrations. The migration also applied to development PostgreSQL. Live customer route returned the expected 401 without authentication, and `/admin/analytics` returned 200. Authenticated interaction review remains unavailable because this browser has no tenant-admin session. The existing development Silver plan has Analytics enabled by local configuration; its stock-plan fixture was verified on the fresh database rather than changing the cafe's plan settings.

## Inventory Phase 0/1 stabilization

The observed Inventory 404s came from a development Nest process started before the new module was registered; direct API requests and `/api/backend` requests both changed from route-level 404 to auth-level 401 after an API restart. The `/tenant/subscription` route was already registered. Inventory now rejects normalized duplicate category/location names, blocks writes to inactive items, rejects duplicate count lines and oversized page numbers, pages count history, and uses movement indexes for recent history and per-balance lookups. The admin UI pages stock/counts and refreshes filtered datasets independently. Migration `1787840000000` is applied in the local development database. An authenticated read-only smoke test through the BFF returned 200 for all seven Inventory GET routes and 400 for invalid category/location POST bodies. The PostgreSQL Inventory integration test, workspace typecheck, full test suite, and production build passed; authenticated visual UI testing remains pending because this browser session has no admin login.

## Inventory Phase 1

Implemented tenant-scoped item/category/location management, stock balances, opening movements, manual adjustments, immutable movement history, overview and physical counts. Inventory endpoints require the configurable entitlement and tenant RBAC. Added migration `1787835600000-InventoryOperations` for categories/count snapshots and protected-owner permission grants. The UI lives at `/admin/inventory` and exposes overview, stock/item management, count entry, movement history, locations, and categories. Count completion rebases each line over movements recorded after its count timestamp and creates reconciliation movements transactionally.

Verification: full API suite passed (92 tests, including PostgreSQL analytics/inventory integration); workspace typecheck and API/web production builds passed. Migrations `1787832000000` and `1787835600000` are applied in the development database, and `/admin/inventory` returns HTTP 200 in the dev web service. Known limits: items, stock rows, and counts are capped to the UI's first 100; history UI filters item/location/type but not dates; count search is deferred. Phase 2 starts with versioned recipes referencing inventory item IDs, per [INVENTORY.md](INVENTORY.md).

Repository checkpoint remains in [CURRENT_STATE.md](CURRENT_STATE.md); this file records the Inventory continuation point requested for the feature work.

## Inventory Phase 2: recipes and versioning

Implemented tenant-scoped optional recipes for menu items and variants, immutable published versions, draft revision checks, exact unit normalization, atomic publication, stable variant identity during menu edits, recipe coverage UI, and menu-editor links. Migrations `1787844000000-CreateInventoryRecipes` and `1787847600000-ProtectPublishedRecipeVersions` enforce tenant-scoped relations, one active and one draft per recipe, unique target/version/component keys, positive quantities, and immutability after publication. Existing Inventory entitlement and `inventory.read`/`inventory.manage` permissions cover every recipe operation. Recipe changes create no stock movements.

Verification: API and web typechecks and production builds passed. The full API suite passed (96 tests; 93 passed, 3 existing analytics database tests skipped); the focused PostgreSQL recipe lifecycle suite passed again after the final migration and service changes. Both migrations are applied to development PostgreSQL, and all 32 migrations show as executed. The local production web preview returned HTTP 200 for `/admin/inventory/recipes`. Compose API/web services could not be refreshed after the Docker build stalled, so the existing Compose route check is inconclusive; authenticated visual UI inspection still needs an admin session.

Continuation: Phase 3 begins in order fulfillment/consumption. Resolve recipes with `RecipesService.resolveActiveRecipe`, prefer exact variant then base item recipe, and persist the applied `recipeVersionId` with the order consumption. Create idempotent `SALE_CONSUMPTION` movements and reversals transactionally; do not infer historical orders from today's active recipe.

## Inventory Phase 3: order consumption and reversal

Implemented `UNDER_REVIEW → PREPARING` consumption and operational cancellation reversal through the canonical `OrderingService.updateStatus` path. Order status, inventory movements/balances, and notification enqueue share the same PostgreSQL transaction. Consumption is inventory-entitlement-aware; missing optional recipes are warned and skipped, while a configured recipe without a valid active version fails atomically. Recipe quantities are normalized exact decimals; variant recipes precede base recipes. Reversal references and restores original consumption movements even if recipes or entitlement later change.

Migration `1787851200000-IntegrateOrderInventory` adds order-line/recipe/reversal references and database uniqueness/validation. Movement rows keep generic source type/ID alongside structured order and recipe references. No old/in-flight accepted orders are backfilled. There is no order edit, partial cancellation, modifier, customer cancellation, refund, or cost snapshot flow; current checkout supports offline payment and no modifiers. Failed transitions can be retried because the order state and stock ledger roll back together.

Verification: migration `1787851200000` is applied; all 33 migrations show as executed. The focused PostgreSQL order-inventory integration passed, including atomic rollback and database duplicate guards. The full API suite passed (98 tests: 95 passed, 3 existing database-dependent tests skipped); API/web typechecks and production builds passed. Live API readiness and `/admin/inventory` returned HTTP 200. Authenticated visual inventory-page inspection remains unverified because this browser session has no admin login.

Continuation: Phase 4 starts with supplier, purchase order, goods receipt, and purchase-cost snapshots. Receipts must post through the same generic immutable movement ledger and transaction-safe balance path; do not couple purchase sources to order-specific references.

## Inventory Phase 4: suppliers, purchasing and goods receiving

Implemented tenant-scoped supplier management, editable draft POs, ordered/partial/received/canceled lifecycle, PO-derived remaining quantities, direct and PO-linked draft goods receipts, explicit over-receive confirmation, immutable posted receipt history, actual unit/line cost snapshots, supplier latest-price lookup, and the Persian RTL purchasing screen at `/admin/inventory/purchasing`. Migration `1787854800000-InventoryPurchasing` adds purchasing records, generic movement source-line/cost snapshots, and per-location moving-average cost; `1787858400000-AuditPurchaseOverReceipt` records the explicit over-receive confirmation.

Receipt posting reuses `InventoryService` inside a transaction. The receipt and its PO are row-locked, inventory balances are processed in stable item/location order, and the PO status is derived from posted receipt lines. Reposting is idempotent. Supplier, PO, item, receipt, line, and location relations are tenant-scoped in both service queries and PostgreSQL constraints. Purchases use integer toman prices; actual unit costs are normalized to base units and the balance weighted average updates only when a receipt posts.

Verification: migrations `1787854800000` and `1787858400000` applied to the development database. The focused inventory/recipe/order/purchasing regression passed (16/16); the final purchasing spec passed (3/3); the full API suite passed (99 passed, 3 skipped); root typecheck and production build passed. Rebuilt development containers are running; API readiness returned HTTP 200 with Postgres, Redis, and object storage healthy, and `/admin/inventory/purchasing` returned HTTP 200. Authenticated visual inspection remains unverified because this browser session has no admin login. Known limits: count-item package conversions, receipt reversal/cost correction, tax/shipping/discounts, and supplier payment/accounting remain deferred. The API/UI require a manual adjustment to correct a physical quantity on a posted receipt; this does not rewrite the receipt or its average cost.

## Inventory Phase 5: waste, low stock alerts and PAR levels

Implemented reasoned tenant/location-scoped Waste Records with draft editing, atomic/idempotent posting to immutable `WASTE` movements, per-line moving-average cost snapshots, unknown-cost handling, and full-record compensating reversal. Added per-location minimum/PAR rules, deterministic status, persistent deduplicated negative/out/low alert episodes updated from the canonical stock movement writer and threshold changes, and current/projected PAR gaps using remaining ordered/partial PO quantity at expected line locations. The existing `/admin/inventory` screen now includes Persian RTL Waste, Alerts and threshold workflows, status/projection in stock, history references, and actionable overview metrics.

Migrations `1787862000000-InventoryWasteAndStockLevels` and `1787865600000-ProtectWasteLifecycle` add expected PO line locations, stock rule/alert/waste tables, ledger references and lifecycle integrity checks. Routes reuse `inventory.read`/`inventory.manage`, tenant context and the shared `inventory` entitlement. `docs/INVENTORY.md` records lifecycle, status, cost, projection, alert and limitation semantics; D-059 and D-060 record the Phase 5 policies.

Verification: both migrations are applied and all 37 migrations show executed. `npm run typecheck` passed for API, web and worker; root `npm run build` passed and the final web build passed after the last UI edit. The full API suite passed with PostgreSQL inventory integrations enabled (103 passed, 3 existing analytics/plan tests skipped). Phase 5 focused database tests passed (4/4), including waste atomicity/idempotency, cost snapshot and preservation, complete reversal, alert transitions/deduplication, tenant isolation, threshold conversion/validation, and PO projection. `git diff --check` is clean. Live API readiness and `/admin/inventory` both returned HTTP 200. An authenticated visual interaction review was not possible because no tenant-admin browser session is available.

Known limits: waste correction is all-or-nothing for a record; unknown average cost remains null; PO projection follows the expected location if a receipt is received elsewhere; alerts are in-app only; there is no expiry automation, recipe-derived waste, forecasts, automatic purchasing, or profitability reporting. Existing count-item package conversion limits remain.

## Inventory Phase 6: recipe costing and menu profitability

Implemented current recipe cost breakdowns and a paginated menu/variant profitability view. Costs use normalized recipe quantities and the active default location's current moving average. Variant offers use their own checkout price and exact variant recipe with base-recipe fallback. Unknown average cost remains null; incomplete recipes never show a complete margin. Historical/draft versions are explicitly labeled current-cost estimates. The UI includes status, search, category, availability and allow-listed sort filters in Persian RTL.

`GET /tenant/inventory/recipes/:id/cost` returns recipe-component cost details, while `GET /tenant/inventory/profitability/menu` returns menu offers and pagination. Both require `inventory.read`, tenant context and the common Inventory entitlement. Migration `1787869200000-RecipeCostingSnapshots` constrains average/sale costs and extends sale reversals to preserve the original cost snapshot. New consumption movements snapshot known average unit costs; older rows remain unknown without backfill. D-061 records default-location and current-estimate semantics; `INVENTORY.md` and `ANALYTICS.md` define the boundary from historical delivered-sales reporting.

Verification: migration 38 applied in development; focused costing and order-consumption/reversal PostgreSQL integration scenarios passed; workspace typecheck and root production build passed; full `npm test` passed (106 passed, 3 existing analytics/plan database cases skipped); rebuilt development API/web containers are running; `/admin/inventory/costing` returned HTTP 200 and the costing API route returned its expected unauthenticated 401. The UI detector returned no findings. An authenticated visual interaction check was unavailable because the browser had no tenant session and showed the OTP login screen.

Known limits: this is material-cost profitability, not accounting profit; labor, rent, tax, fee, waste allocation, modifiers, historical cost replay, forecasts, and multiple-location profitability remain outside scope. Earlier sale movements have no backfilled cost.

Continuation: Inventory Phase 8 is implemented. Phase 9 has not been scoped; retain the listed Phase 8 limits until a product requirement defines the next workflow.

## Inventory Phase 7: actual vs theoretical usage and variance

Implemented a read-only count-to-count report at `/admin/analytics`. Actual depletion uses explicit physical counted quantities and per-item `counted_at` bounds, trusted posted receipts, supported trusted non-consumption movements and the closing physical quantity. Theoretical use nets only source-valid signed `SALE_CONSUMPTION` / `SALE_REVERSAL` movements, preserving each order's recipe version; known waste nets valid Waste and full-record reversal movements. Manual adjustments, opening balances and count reconciliation are excluded and visibly flagged. Missing count lines are non-calculable; zero counts remain valid. Source-invalid receipt/sale/waste movements are omitted and shown in drill-down.

Added three tenant-scoped Analytics endpoints, DTO validation, both feature/permission requirements, one grouped PostgreSQL report query, paginated items, summary/coverage, item movement drill-down, and Persian RTL UI with cafe-timezone dates. Migration `1787872800000-InventoryVarianceMovementIndex` adds a tenant/location/item/time lookup index; no business tables changed. Historical variance cost is unavailable because current average balance cost cannot recover the cost at a past count boundary. Order-line coverage uses creation time as a proxy because accepted-at event history is not stored. The report never creates stock or waste corrections.

Verification: migration 39 is applied; `migration:show` lists all migrations applied. The focused rollback-only PostgreSQL variance suite passed (4/4). The complete API suite passed (110 passed, 3 skipped); the skipped database-only Analytics/plan cases were not pointed at the development database because its Silver plan settings are customized. Root workspace typecheck and production build passed (`NODE_ENV=production npm run build`); `git diff --check` passed. The Analytics page returned HTTP 200 and the new API route returned the expected unauthenticated 401 after restarting the local API. An authenticated visual interaction check remains pending because this browser has no tenant-admin session.

## Inventory Phase 8: batches, lot traceability and expiry

Implemented optional item-level batch/expiry settings, tenant/location-scoped batches, normalized receipt and opening batch splits, movement-backed remaining quantities, FEFO/FIFO order allocation, exact-batch sale and Waste reversals, batch-aware stock counts, audited lot/date corrections, and deduplicated in-app expiry alerts. Existing balances remain unallocated when tracking is enabled; no historical lots are fabricated. Expired stock is excluded from automatic order allocations and is never automatically written off. Physical allocation does not change the Phase 4/6 moving weighted-average costing rules.

Migration `1787876400000-InventoryBatchesAndExpiry` adds the batch table, movement links/projection guard, receipt batch details, count/waste allocation references, and expiry alert identity. Batch, receipt, waste, count, movement, overview, and alert views are wired into the Persian RTL inventory and purchasing screens. Phase 7 Analytics now aggregates allocation-level count rows back to item/location quantities and continues to read ledger movements without creating corrections.

Verification: migration 40 applied locally and `migration:show` confirms all 40 migrations executed. Workspace `npm run typecheck`, root `npm run build`, and the API suite with Inventory PostgreSQL integration enabled passed (110 passed, 3 unrelated Analytics/subscription database cases skipped). Focused batch/order/receipt PostgreSQL cases passed (5/5), and the Impeccable UI detector returned no findings. A locally launched production web server returned HTTP 200 for `/admin/inventory`. The running dev containers bind-mount the current source; an internal container check returned HTTP 200 for `/admin/inventory` and the expected unauthenticated 401 for the protected batch API. Rebuilding images was blocked by a Docker Hub 403 while fetching the Node base image, and host-published ports returned empty replies. Authenticated visual interaction remains unverified because no tenant-admin browser session is available.

## Inventory Phase 9: smart purchasing and supplier price intelligence

Implemented a paginated replenishment assistant that reuses Phase 5 stock status and committed PO projection, adds expiry-aware usable quantity for tracked expired lots, and returns a deterministic gap to configured PAR. A tenant/item supplier preference supports one preferred active supplier, existing compatible purchase units, and MOQ normalized to base quantity. Posted receipt lines remain the only supplier-price source; each supplier/item/receipt is quantity-weighted to base units before comparing the latest and immediately previous receipts. Comparisons retain inactive suppliers and suppliers with no history as explicit rows, with factual percent change and a paginated receipt-history drill-down.

Migration `1787880000000-InventorySupplierPreferences` adds tenant-safe supplier/item preferences and at-most-one preferred supplier per tenant/item. New APIs are `GET /tenant/inventory/replenishment`, `GET /tenant/inventory/items/:itemId/supplier-prices`, `GET /tenant/inventory/items/:itemId/supplier-price-history`, and `PUT /tenant/inventory/items/:itemId/suppliers/:supplierId/preference`. The purchasing page now includes a Persian RTL replenishment tab and uses the existing Phase 4 PO endpoint for explicit draft creation; draft lines are editable and never affect stock or cost.

Package conversion is intentionally limited to the existing physical-unit converter. Decimal kg/l quantities are preserved; custom bag/carton/box conversions and integer-pack rounding are not guessed. Lead time, consumption context, recent weighted supplier averages, minimum order value, automatic procurement, and historical spend BI remain deferred. Current stock status and Phase 5 projected PAR remain gross operational projections; the Phase 9 purchase gap separately excludes known expired batch quantity while surfacing the expiry warning.

Verification: Phase 9 migration 41 is applied locally and `migration:show` confirms all 41 migrations are executed. API and web typechecks passed; `NODE_ENV=production npm run build` passed for the workspace. The serial full API suite passed (111 passed, 3 skipped); the skipped cases require the separate Analytics/subscription integration setup. Phase 9 purchasing integration assertions ran against PostgreSQL, including receipt-level weighted price history, partial PO quantity, MOQ, expiry exclusion, tenant scope, and no stock-movement side effects. The default parallel test run once deadlocked the existing order-consumption integration case while database fixtures overlapped; that case passed alone and in the serial suite. The production purchasing page returned HTTP 200; authenticated UI interaction remains unverified because this browser has no tenant-admin session. `git diff --check` passed. Phase 10 should first define and validate a dated item/location sale-consumption series from source-valid sale-consumption movements and reversals (with recipe-version context), then evaluate a deterministic baseline separately from PAR assistance.

## UCafe Analytics Phase 6: reservation analytics

Implemented `GET /api/v1/tenant/analytics/reservations` and the Persian RTL Reservations tab. The report separates creation, scheduled demand, and terminal-outcome times; adds outcome rates, scheduled guest/party-size metrics, lead times, weekday/hour/heatmap distributions, and peak periods. The shared Phase 2 heatmap now accepts generic numeric measures. Migration 44 adds tenant-scoped creation and status-event indexes. Definitions and data limits are in [ANALYTICS.md](ANALYTICS.md); D-066 records the state/rate semantics.

Verification: all 44 migrations are applied; workspace typecheck and production build pass; focused Analytics, reservation-time, tenant-authorization, and subscription-lifecycle tests pass (32/32), including the PostgreSQL reservation integration case. The full API suite produced 112 passes, 1 failure, and 7 skips. Its only failure is the Silver-plan-defaults assertion against the locally customized Silver plan; legacy database-only cases are skipped without their integration setup. The admin Analytics page returned HTTP 200 and the protected endpoint returned the expected unauthenticated 401. Authenticated UI interaction was unavailable because no tenant-admin browser session is present.

## UCafe Discounts & Promotions Phase 0

Implemented tenant-scoped product and category promotions with percentage, fixed-amount, and fixed-price rewards; scheduling, priority, activation, soft archive, target validation, and database tenant guards. Public menu prices show original/final/discount values, carts request a server quote, and order creation recalculates inside its transaction. Order and line snapshots preserve gross values and the winning promotion's identity and reward. Management uses existing `menu.read` and `menu.manage` permissions. See [DISCOUNTS.md](DISCOUNTS.md) and decision D-067.

Migration `1790424000000-CreatePromotionsAndOrderSnapshots` was applied, reverted, and reapplied against the development PostgreSQL database; `migration:show` lists it as executed. It backfills existing orders and lines as full-price, zero-discount records. Existing offline payable totals remain unchanged.

Verification: workspace `npm run typecheck`, workspace production `npm run build`, and the API build after the final Nest injection fix passed. The full API suite passed (111 passed, 13 database-backed cases skipped); focused promotion-pricing tests passed (4/4); `git diff --check` passed. Compose API readiness reports Postgres, Redis, and object storage healthy. A temporary 10% product promotion produced matching public menu, API quote, BFF quote, and browser cart values: 120,000 toman gross, 12,000 discount, and 108,000 payable. The fixture was deleted and the quote returned to 120,000 with zero discount. A forged price field was rejected with 400. The admin promotions, menu, cart, and checkout routes returned 200. The discounted cart reconciled at 390×844 and 1440×900 RTL viewports with no horizontal overflow. The owner page reached its OTP login screen; authenticated promotion CRUD and order submission remain unverified because no tenant-admin or customer session was available.
